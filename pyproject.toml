[project]
name = "peer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.5.0",
    "einops>=0.8.1",
    "matplotlib>=3.10.8",
    "transformers>=4.57.6",
]
